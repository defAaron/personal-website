/* Generated from Hacks_and_stuf project docs */
const AARON_WORK_WRITEUPS = {
  "skyeye": {
    "overview": "SkyEye is an AI-assisted triage tool for missing-person search and rescue. It helps teams answer two urgent questions in the first hours of a search: where should we concentrate effort, and which drone frames deserve a human look first.\n\nThe product couples plain-language report intake with a map-based search ring, then runs person detection only on drone-altitude photographs, the imagery SAR teams actually review. Every output is a lead to verify, not a confirmed find. SkyEye supports dispatch, SAR, and law enforcement. It compresses hours of manual scanning into minutes of ranked candidates.",
    "problem": "The opening hours after someone goes missing are chaotic. Reports arrive as scared, unstructured text. Search areas start as guesses. When a drone goes up, someone still has to stare at frame after frame for a person-shaped speck in trees, water, or trail.\n\nCommercial satellite and basemap imagery typically resolves around 30–50 cm per pixel. From nadir, a person is roughly half a meter across, often one to two pixels. Detectors cannot reliably find a human at that scale. Map tiles are the wrong input for person detection under real GIS constraints.\n\nSearch professionals use literature-backed heuristics, Lost Person Behavior, to size initial rings from subject type and elapsed time. Tools that ignore that literature and over-promise automated finds erode trust when it matters most.",
    "solution": "SkyEye separates where to look from what to look at.\n\nReport → search area. A bystander or coordinator describes what they know in natural language (age, clothing, last-known place, time). An LLM extracts structured fields. Geocoding places the last-known point on a map. A simplified Lost Person Behavior table sizes a search ring around that point, scaled with elapsed time so the ring grows sensibly rather than doubling whenever time doubles.\n\nDrone photo → ranked candidates. Wilderness or SAR-style photographs go through a sliding-window detector tuned for small, aerial subjects. Detections merge across tiles, score by confidence, and appear as overlays and map pins when imagery is georeferenced. The operator reviews ranked boxes. The interface avoids “found” language. A persistent safety banner reminds users to contact 911 or local SAR and treat every pin as unverified.\n\nDetection runs on uploaded or demo drone photos at altitudes where people occupy dozens of pixels. It does not run on map tiles.",
    "architecture": "SkyEye is a split stack: a Vite + React + TypeScript operator console and landing page, a Flask API, and production deploy on Vercel (UI) and Render Docker (API). Heavy inference bypasses serverless timeouts by calling Render directly from the browser for api/detect.\n\nFree-text reports hit api/extract. Gemini Flash is the primary model, with ordered fallbacks across Flash variants, then Groq GPT-OSS models if Gemini rate-limits or errors. The pipeline returns typed JSON: last-known location string, elapsed hours, clothing, subject category, and related fields. That structure drives geocoding and the LPB radius instead of free-form prose in the UI.\n\nServer-side keys, rate limits per client IP, and outbound caps on LLM and geocoding calls keep demo abuse from burning quotas. Detection still runs without LLM keys. The map ring path needs geocoding.\n\nGeocoding API (server-side, Flask api/geocode) turns extracted place names into lat/lng for the search ring center.\n\nMaps JavaScript API (browser, via @vis.gl/react-google-maps) renders the ring, center pin, and optional detection pins. Keys are split: referrer-restricted key in the frontend for JS; server key for geocoding, aligned with Google’s recommended pattern for capability and exposure.\n\nSearch radius comes from a simplified 50th-percentile Lost Person Behavior table in code: base radius by category, scaled by √elapsed time with clamp bounds so rings stay practical for dispatch.\n\nThis path is the core engineering bet.\n\nWhy tiling. In drone nadir photos, people are often 30–100 pixels tall. A single resize of a large image to 640×640 shrinks those subjects below what a COCO-pretrained YOLOv8n can see. SkyEye keeps native-resolution crops: a sliding grid with configurable tile size and overlap so subjects on tile boundaries still appear fully in at least one window. Stride is tile_size × (1 − overlap); edge tiles flush to image borders.\n\nPer-tile inference. Each tile is letterboxed to the network’s 640×640 input (Ultralytics-style padding and scale). ONNX Runtime runs yolov8n.onnx with no PyTorch import at request time. Production on a 2 GB container OOM-killed on full PyTorch even for a small model. The Docker image bakes the ONNX export in a build stage; runtime is numpy + ONNX Runtime + one Gunicorn worker.\n\nPost-process. Raw outputs decode to boxes in letterbox space, map back to tile coordinates, then project to full-image space. Detections from overlapping tiles merge with IoU and containment rules so the same person is not listed five times. Class filter keeps person (COCO id 0); confidence threshold trades recall against false positives.\n\nOperational levers. TILE_SIZE is explicitly a recall knob: larger tiles (e.g. 320 px) upscaled more aggressively into 640×640, which measured 4/10 → 7/10 recall on a controlled scale test at roughly 2.5× latency and ~5× false positives. The README and UI document that tradeoff openly.\n\nLimits baked into the design. Pretrained YOLOv8n has a practical ~60 px floor for reliable hits; HERIDAL-class imagery often sits at 30–60 px. Aerial fine-tuning on SAR data is the next step for that gap. Pose failures (e.g. prone subject on a board, top-down) tiling alone cannot fix because the prior favors upright COCO person. Demo fixtures include a known miss on purpose.\n\nOther API surfaces: api/samples serves an openly licensed demo corpus; api/health for ops; upload path for operator JPEG/PNG with guards on decompression bombs.",
    "whyItWorks": "SkyEye respects resolution physics. Map layers answer where the LKP is and how big the initial ring should be, questions geocoding and LPB already support. The detector only sees pixels where a person is plausibly visible.\n\nThe two-loop UX matches how teams think: intake and geography first, then imagery review. Ranked scores and explicit “lead to verify” copy align the product with SAR doctrine rather than consumer “found them” apps.\n\nTiling + ONNX makes small-object recall reachable on CPU within a ~5–8 s worst-case demo fixture on a laptop, inside a 30 s time-to-first-lead target, without GPU or PyTorch memory blowups on Render.\n\nMeasured honesty (7/8 demo fixtures behaving as expected, two true-negative canopy images with zero false positives, documented 60 px floor and one pose miss) builds credibility with judges and operators who have seen inflated CV demos before.",
    "learned": "Remote sensing has a resolution floor that prompts cannot override. When GSD puts a human at one pixel, the product needs a different sensor altitude or modality instead of a larger model alone.\n\nSAR has literature worth encoding. A simplified Lost Person Behavior ring beats an arbitrary “5 km circle” and signals that the tool was built with operators in mind.\n\nSmall-object detection is a tiling and prior problem, and confidence threshold alone is insufficient. Lowering conf to 0.05 recovered zero sub-60 px subjects and flooded false positives. Overlap, tile size, and eventually aerial fine-tune are the real levers.\n\nProduction shape mattered as much as the algorithm. Export-once ONNX, no torch in the serving image, thread limits, and direct browser→API detect routing carried “works on my laptop” into Docker and Hobby-tier timeouts.\n\nThe most important rescue-facing UI choice was refusing certainty: ranked leads with scores over a green checkmark, with an emergency banner that stays visible.",
  },
  "surpluslink": {
    "overview": "SurplusLink is a mobile-friendly web app that connects restaurants and pantries with people nearby who can claim edible surplus before pickup windows close. Donors photograph leftovers and get AI-assisted listing fields they confirm before publishing. Recipients browse a live map, reserve portions, and plan an ordered multi-stop pickup run. The project ships as a full-stack Next.js application on Vercel with Supabase Postgres, local computer vision, and open routing APIs.",
    "problem": "Restaurants and food pantries throw away edible surplus every day while people a few blocks away face food insecurity. The gap is rarely about willingness to share. It is about discovery, timing, and logistics. Surplus spoils quickly. Staff do not have time to write detailed listings by hand. Recipients who hear about a donation often do not know what is available, where to go, or how to visit several stops efficiently. Existing channels are fragmented: phone trees, social posts, and one-off spreadsheets that go stale within hours.",
    "solution": "SurplusLink gives each side a focused workflow. Donors create an organization profile with a pickup location, snap a photo of surplus food, review suggested title, categories, allergens, and quantity, then set a pickup window and publish. Listings appear on a public explore board with a map and list view. Recipients filter by distance, search, and allergens they need to avoid, claim a number of portions, and see donor contact details only after a successful claim. When someone holds reservations at multiple stops, the app orders those stops and draws a driving route so pickup runs feel planned instead of improvised. Listings past their end time expire automatically so the board stays honest about what is still available.",
    "architecture": "The client is Next.js 15 (App Router) with TypeScript and a mobile-first UI. Auth.js handles credential-based sessions with separate donor and recipient roles. Prisma talks to Supabase Postgres using a transaction pooler in production and a direct URL for schema migrations.\n\nLocal vision with Hugging Face Transformers and ONNX. Listing creation sends the photo to a server route that runs a Food-101 image classifier through @huggingface/transformers, using the onnx-community/swin-finetuned-food101-ONNX model with quantized (q8) weights on CPU via onnxruntime-node. The pipeline loads once per runtime and caches weights in a writable directory (project .cache locally, /tmp on Vercel where the deployment bundle is read-only). The model returns the top five labels and scores. A mapping layer turns the winning dish label into a human-readable title, category tags, conservative allergen hints derived from the dish name (not from pixel-level allergen detection), and a suggested portion count. Predictions below a confidence threshold trigger manual entry so donors are not misled by noise. Per-user VISION\\_ quotas and an offline fallback keep serverless cold starts and abuse from blocking the flow: donors always reach a confirm-and-publish screen.\n\nNearest-neighbor search and routing. Multi-stop pickup is a small traveling-salesperson problem. The server first builds stop order with nearest-neighbor: starting from the recipient’s origin, repeatedly visit the closest unvisited donor until every claim location is covered. That greedy tour is refined with 2-opt, swapping segments of the path when straight-line distance (Haversine) improves. Stop order is fixed before road geometry is fetched. The handler then calls the public OSRM driving API for leg durations and a GeoJSON polyline. If OSRM times out or fails, the same ordered stops return with Haversine-based duration estimates and a straight-line geometry marked as degraded so the UI still shows a sensible sequence.\n\nClaims and map. Portion claims run inside a Prisma interactive transaction so inventory cannot oversell when two recipients act at once. Leaflet and OpenStreetMap tiles power the explore map. Photos persist under public/uploads in development and as data URLs on Vercel when filesystem writes are unavailable.\n\nBrowser → Next.js route handlers\n\n              ├── Supabase Postgres (Prisma)\n\n              ├── Food-101 ONNX (@huggingface/transformers)\n\n              └── OSRM (with geometric fallback)\n\nBrowser → Leaflet / OSM",
    "whyItWorks": "The product treats AI as assistive and keeps humans in the loop. Staff must confirm allergens and portions before anything goes live, which matches how real kitchens think about liability and accuracy. Local ONNX classification avoids recurring cloud vision fees and keeps photos on the application server for the analyze step, which matters for a hackathon-scale budget and for demos that need to work without API keys.\n\nThe routing stack matches the problem size. Nearest-neighbor plus 2-opt is fast for a handful of stops and good enough for same-day food rescue. OSRM adds realistic drive times when the network cooperates; the geometric fallback preserves ordering when it does not.\n\nTransactional claims and explicit expiry align the database with what recipients see on the map. Privacy choices (donor phone only after claim, no recipient identity on the public board) make the public explore view safe to share while still enabling coordination after someone commits to a pickup.",
    "learned": "Running a Transformers.js ONNX model inside a serverless host taught me to treat model load as infrastructure: singleton caching on globalThis, writable cache paths, and a first-class offline path when weights cannot load or confidence is low. Food-101 identifies dishes, not allergens, so the useful layer was honest heuristics on the label plus clear copy that staff must verify.\n\nImplementing pickup routing clarified when to reach for heavy optimizers versus simple geometry. For two to five stops, nearest-neighbor with 2-opt plus an external router is easy to explain, test, and ship under a two-second demo budget, especially with a timeout and degraded response.\n\nEnd-to-end, the MVP reinforced that food rescue software is coordination software: tight pickup windows, inventory that must not go negative, and a map that has to reflect reality the moment someone leaves home to collect food that will not wait until tomorrow.",
  },
  "honeydesk": {
    "overview": "HoneyDesk is a student-focused deception and education platform built for STEMist Hacks IV. It deploys credible fake student surfaces (school portals, scholarship forms, Discord verify pages, and more) so practice interactions can be captured safely, analyzed in real time, and turned into guidance a teenager can actually use. Operators watch a live dashboard as each trap fires; students get short, plain-English briefs; school IT can download structured incident data in one click.",
    "problem": "Students face scams that look boring on paper and feel urgent in the moment. Fake Canvas or Google logins harvest credentials. “You’ve been selected” aid and scholarship forms push for SSNs and bank details under time pressure. Discord and Instagram “verify your account” pages aim at session tokens and social accounts. Typosquat domains and fake internship offers add more paths to the same outcome: stolen accounts, identity theft, and panic.\n\nMost defensive tools stop at a link score or a generic warning. That helps, yet it rarely shows what an attacker is trying to do across a full interaction: which fields they want, which psychological lever they pull, and what a student should do in the next five minutes. Schools and clubs need something shareable after an incident, not only a screenshot in a group chat.\n\nHoneyDesk treats the problem as both security and literacy: make the trap visible, label the technique, explain the risk in age-appropriate language, and give IT a portable export.",
    "solution": "HoneyDesk inverts the usual phishing story for defense. Instead of waiting for a real scam to land in an inbox, you open or share a practice decoy that mirrors what teens already see. When someone submits the form (in a demo, that is you or a judge), the backend captures redacted structural signals only, runs a fixed analysis pipeline, and surfaces the result on a gated operations dashboard.\n\nThe flow is designed to land in under ninety seconds:\n\n1. Sign up and open the dashboard.  \n2. Submit on a decoy such as the student portal or scholarship form.  \n3. Watch the event appear with technique, severity, and pipeline steps.  \n4. Read the victim brief and optional action list.  \n5. Replay seeded scenarios (SC-1, SC-2, SC-3) for a consistent demo.  \n6. Export a STIX-shaped JSON bundle for school IT.\n\nMultiple decoy types (Wi‑Fi captive portal, SSO, bank alert, Zoom join, FAFSA correction, package hold, textbook buyback, internship offer, social verify, and others) map to distinct scam classes so drills stay varied and realistic.",
    "architecture": "The project is a monorepo with a FastAPI backend and a Next.js App Router frontend, deployed via Render with SQLite for the MVP store.\n\nFrontend\n\nLanding and marketing pages use React and shadcn/ui, with session-based auth (sessionStorage bearer tokens) gating the dashboard and decoys.  \nThe hero uses Three.js directly (not React Three Fiber): a WebGL scene with a low-poly “hoodie at the desk” figure, laptop with emissive screen, soft shadows, mouse-driven parallax on the scene, subtle idle motion, and floating particle “code” motes. The canvas resizes with ResizeObserver, caps pixel ratio for performance, and tears down the renderer on unmount to avoid leaks.  \nDecoy routes live under /decoy/*; the dashboard polls /events and /stats for near-live updates.\n\nBackend and data flow\n\nDecoy submit → POST /capture → Classify → Enrich → Brief → Persist (SQLite)  \nDashboard    → GET /events, /stats, /export/stix/{id}  \nReplay       → POST /simulate (same pipeline as live capture)\n\nClassification (deterministic “NLP”)\n\nBefore any model runs, a rules-first classifier inspects only safe structural fields: decoy type, field names present, scenario metadata, and similar signals. It assigns techniques from a controlled enum (credential_harvest, urgency_pii_scam, social_verify, typosquat, bot_probe, unknown) plus severity and human-readable reasons. User-supplied secret values never get interpolated into explanations, which keeps logs and UI copy safe even when captures are adversarial.\n\nEnrichment\n\nA network-free enrich step derives data targeted categories (school email, password, SSN, bank account, Discord account, and so on) from field keys and decoy context, and attaches demo geo labels for map-style storytelling when needed.\n\nBrief generation (NLP / LLM layer)\n\nThe brief step turns classification output into a victim-ready paragraph (120 words max) and 2–3 concrete actions:\n\nCached briefs for seeded scenarios keep replays deterministic.  \nOptional LLM calls (OpenAI-compatible API, JSON response format, temperature 0\\) receive only sanitized enums and targets, never raw credentials.  \nTemplate fallbacks per technique if the network or model fails.\n\nDesign rule from the architecture docs: rules classify first; the language model explains second.\n\nSecurity and tenancy\n\nCapture, simulate, reads, and export require authentication and are scoped per user. Passwords and sensitive fields are redacted at the capture boundary; the pipeline stores flags and hashes, not plaintext secrets. Rate limits, security headers, and optional simulate tokens harden the API in production.",
    "whyItWorks": "HoneyDesk works because it optimizes for demo truth and student clarity at the same time.\n\nThe spring moment is visceral: a credible decoy, a real submit, and a dashboard row that “detonates” with visible pipeline steps (Capture → Classify → Enrich → Brief). Judges and learners can name each stage instead of staring at a single chat bubble.\n\nThe classifier is fast and auditable. Technique tags come from explicit rules tied to decoy design, so seeded scenarios hit the expected labels every time. That reliability matters in a hackathon room and in a classroom.\n\nThe brief layer is constrained. Word limits, JSON schema, allowlisted inputs, and cached copy prevent runaway or alarmist model output while still allowing live LLM briefs when an API key is present.\n\nEthics are product features, not footnotes: training-only copy on landing and dashboard, redaction by default, and replay that never depends on tricking random people online.\n\nFinally, STIX-shaped export bridges the gap between “my kid got phished” and “here is something IT can ingest,” which completes the school-facing story.",
    "learned": "Building HoneyDesk reinforced that deception for defense still demands strict data hygiene. The most important engineering choice was failing closed on secrets: redact early, never echo passwords in briefs or logs, and treat every capture payload as hostile.\n\nI learned to split “understanding” from “explaining.” Deterministic classification carries the security semantics; the LLM’s job is translation into calm, actionable copy for ages 13–19. Mixing those roles produces flaky demos and unsafe prompts.\n\nThree.js on a marketing hero taught practical WebGL hygiene: limit pixel ratio, use simple geometry and standard materials, animate with requestAnimationFrame, and dispose on cleanup so a landing page does not become a memory leak.\n\nSeeded scenarios and cached briefs are not shortcuts; they are how you ship AI features that still work when Wi‑Fi drops or the API rate-limits you. The same pipeline for live capture and simulate keeps the mental model honest.\n\nLastly, student scams are a product design problem. Credibility comes from specific surfaces (Canvas, Wi‑Fi portal, Discord verify), not from generic “login” pages. Matching decoy fidelity to real teen workflows made classification and education land harder than any single model upgrade.",
  },
};
